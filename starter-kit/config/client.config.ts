// Config activa del sitio. En este repo template apunta a un preset de ejemplo
// para poder correr `npm run dev` y ver una demo funcionando.
//
// `npm run new-client -- --name "..."` sobrescribe este archivo con el contenido
// completo (no una re-exportación) del preset elegido, para que quede autocontenido
// en el branch de ese cliente.
export { default as clientConfig } from "./presets/restaurante.config";
